<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Styles/Generated/UiTokens.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="AccordionStyle" TargetType="Expander">
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border x:Name="Chrome"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="HeaderButton"
                                          MinHeight="{DynamicResource ControlMinHeight}"
                                          Background="Transparent"
                                          Foreground="{DynamicResource TextBrush}"
                                          BorderThickness="0"
                                          FocusVisualStyle="{x:Null}"
                                          Cursor="Hand"
                                          Focusable="True"
                                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="HeaderChrome"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="9"
                                                Padding="14,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter VerticalAlignment="Center"
                                                                  RecognizesAccessKey="True"
                                                                  TextElement.FontSize="15"
                                                                  TextElement.FontWeight="SemiBold" />
                                                <Path x:Name="Chevron"
                                                      Grid.Column="1"
                                                      Width="10"
                                                      Height="6"
                                                      Margin="14,0,1,0"
                                                      VerticalAlignment="Center"
                                                      Data="M 1,1 L 5,5 L 9,1"
                                                      Fill="Transparent"
                                                      Stroke="{DynamicResource MutedTextBrush}"
                                                      StrokeThickness="1.75"
                                                      StrokeStartLineCap="Round"
                                                      StrokeEndLineCap="Round"
                                                      StrokeLineJoin="Round"
                                                      Stretch="None"
                                                      IsHitTestVisible="False" />
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="HeaderChrome" Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsKeyboardFocused" Value="True">
                                                <Setter TargetName="HeaderChrome" Property="BorderBrush" Value="{DynamicResource FocusBrush}" />
                                                <Setter TargetName="HeaderChrome" Property="BorderThickness" Value="1" />
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="HeaderChrome" Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                                                <Setter TargetName="Chevron" Property="Data" Value="M 1,5 L 5,1 L 9,5" />
                                                <Setter TargetName="Chevron" Property="Stroke" Value="{DynamicResource AccentBrush}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <ToggleButton.Content>
                                    <ContentPresenter Content="{TemplateBinding Header}"
                                                      ContentTemplate="{TemplateBinding HeaderTemplate}" />
                                </ToggleButton.Content>
                            </ToggleButton>
                            <ContentPresenter x:Name="ExpandSite"
                                              Grid.Row="1"
                                              Margin="{DynamicResource InsetLg}"
                                              ContentSource="Content" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter TargetName="ExpandSite" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="True">
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Chrome" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BoundedAccordionStyle"
           TargetType="Expander"
           BasedOn="{StaticResource AccordionStyle}">
        <Setter Property="VerticalAlignment" Value="Top" />
        <Style.Triggers>
            <Trigger Property="IsExpanded" Value="True">
                <Setter Property="VerticalAlignment" Value="Stretch" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PreferencesAccordionStyle"
           TargetType="Expander"
           BasedOn="{StaticResource AccordionStyle}" />

    <Style x:Key="PreferencesNestedAccordionStyle"
           TargetType="Expander"
           BasedOn="{StaticResource PreferencesAccordionStyle}" />
</ResourceDictionary>
