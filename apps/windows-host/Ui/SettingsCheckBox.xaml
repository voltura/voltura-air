<UserControl x:Class="VolturaAir.Host.Ui.SettingsCheckBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             HorizontalAlignment="Left">
    <UserControl.Resources>
        <Style x:Key="SettingsCheckBoxToggleStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="24" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border x:Name="Box"
                                    Width="22"
                                    Height="22"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    Background="{DynamicResource SurfaceRaisedBrush}" />
                            <Path x:Name="Check"
                                  Data="M 5 11 L 9 15 L 17 7"
                                  Stroke="{DynamicResource AccentTextBrush}"
                                  StrokeThickness="2.2"
                                  StrokeEndLineCap="Round"
                                  StrokeStartLineCap="Round"
                                  Visibility="Collapsed" />
                            <ContentPresenter Grid.Column="2"
                                              VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border x:Name="Chrome"
            Background="{DynamicResource SurfaceBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12"
            Cursor="Hand"
            PreviewMouseLeftButtonDown="OnChromePreviewMouseLeftButtonDown">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsKeyboardFocused, ElementName=Toggle}" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource FocusBrush}" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding IsMouseOver, ElementName=Root}" Value="True">
                        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <CheckBox x:Name="Toggle"
                      Content="{Binding Label, ElementName=Root}"
                      IsChecked="{Binding IsChecked, ElementName=Root, Mode=TwoWay}"
                      Style="{StaticResource SettingsCheckBoxToggleStyle}"
                      VerticalAlignment="Center" />
            <Button x:Name="InformationButton"
                    Grid.Column="2"
                    Content="ⓘ"
                    Style="{DynamicResource SettingsInformationButtonStyle}"
                    ToolTip="{Binding InformationAccessibleName, ElementName=Root}"
                    ToolTipService.InitialShowDelay="350"
                    AutomationProperties.Name="{Binding InformationAccessibleName, ElementName=Root}"
                    Click="OnInformationClick" />
        </Grid>
    </Border>
</UserControl>
